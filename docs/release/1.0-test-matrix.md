# TableTop DM 1.0 Test Matrix

Status: release-gate test plan. Every blocker or critical gap found here must be closed before a 1.0 tag.

## Required Command Gates

```powershell
python -m py_compile app.py services/**/*.py shared/**/*.py
node --check static/js/app.js
pytest tests/services tests/contracts tests/integration -vv
pytest tests/e2e -rs -vv
```

Additional release-candidate gates as they land:

```powershell
pytest tests/e2e/test_v1_golden_path.py -vv
pytest tests/integration/test_route_auth_matrix.py -vv
pytest tests/integration/test_save_load_roundtrip.py -vv
pytest tests/integration/test_v1_perf_smoke.py -vv
```

## Matrix

| Area | Required proof | Current coverage | Release gap |
|---|---|---|---|
| Boot/readiness | `/health`, `/readyz`, `/`, `/game`, `/control`, `/help` are reachable and diagnostic. | `tests/integration/test_readiness.py`, `tests/e2e/test_routes_smoke.py` | Add release packaging boot checks for shortcut/manual modes. |
| Campaign lifecycle | DM can create, update, resume, delete/archive campaign without data drift. | Control-plane contracts/integration | Add golden-path browser coverage. |
| Session lifecycle | Create/resume/pause/end/archive works and story state survives. | Session integration tests, control-plane integration | Add full session lifecycle to golden path. |
| Principal identity | Anonymous/non-member rejected; player cannot impersonate DM/other player. | Socket, continuity, player-state tests | Phase 43 local identity/join-code hardening. |
| Player state | One snapshot includes identity, controls, visible world, narrative state, legal actions, event cursor. | `tests/integration/test_player_state_snapshot.py`, `tests/e2e/test_player_state_boot.py` | Add reconnect/cursor-gap tests. |
| Ledger visibility | Principal reads only visible ledger rows. | RLS/security integration tests | Extend route matrix to all read APIs. |
| Realtime visibility | DM-only, public, principal-scoped, spatial delivery reaches only correct rooms. | `tests/integration/test_socket_visibility.py` | Add snapshot/delta parity and reconnect recovery. |
| Maps/movement | Player can move controlled entity; visible map updates; out-of-scope entities stay hidden. | Mechanics/spatial tests, player-state spatial tests | Add golden-path movement and reload check. |
| POI discovery | In-radius POI visible; out-of-radius/hidden POI absent. | Player-state integration tests | Add browser assertion on map UI. |
| Combat basics | Active turn, legal action hints, attack, end turn, NPC turn advance. | Mechanics, state-machine, RG flow tests | Add golden-path combat browser flow. |
| Chat/narration | Chat creates ledger event and scoped realtime; narration does not mutate canonical state directly. | RG flow, socket visibility | Add player/DM transcript-free chat golden path. |
| Story State Board | Player projection omits DM notes; DM can edit/review canonical story state. | Player-state and Session Intel tests | Add DM review UX hardening tests. |
| RAG baseline | Upload, chunk, retrieve, visibility filter, citations. | RAG service tests, control-plane integration | Add save/load RAG preservation/failure behavior. |
| Session Intel | Extract -> proposed patches -> review -> apply -> recaps/continuity. | Session Intel service tests, visibility matrix | Add browser review-packet smoke. |
| Save/load | Wrong passphrase, import/export, replace modes, principal remap, ledger visibility preserved. | Save services exist | Add Phase 47 torture suite. |
| Docs | README/wiki/security/release docs match shipped behavior. | Manual only | Add docs reality checklist in Phase 49. |
| Performance | Player state, RAG query, save/load, boot are inside local targets. | None | Add Phase 50 perf smoke. |

## Golden Path Acceptance

The 1.0 golden-path E2E must prove:

1. Clean stack boots.
2. Dashboard opens.
3. DM creates or selects a campaign.
4. Session exists and player can join.
5. `/game` loads `player_state`.
6. Player moves on map and sees a scoped update.
7. Player discovers visible POI.
8. Chat creates visible event.
9. Combat action and turn advance work.
10. DM-only state never appears in player UI.
11. Save/export works.
12. Restart/resume returns equivalent player-visible state.

## Failure Artifacts

E2E and RC scripts should preserve, at minimum:

- `test-results-e2e/e2e-readyz.json`
- `test-results-e2e/e2e-root-response.html`
- browser screenshots/videos/traces on failure
- server log for the live test app
- route response payload for failed auth/visibility tests
